<% if (locals.Recipe) { %>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">Home</a></li>
            <li class="breadcrumb-item active" aria-current="page">
                <%-Recipe.name %>
            </li>
        </ol>
    </nav>
    <div class="row p-4 p-md-2">
        <div class="col-12 col-md-4">
            <img src="/uploads/<%-Recipe.image %> " class="img-fluid sticky-top" style="top:20px"
                alt="<%-Recipe.name %>">
        </div>

        <div class="col-12 col-md-8">
            <h1 class="text-info">
                <%-Recipe.name %>
            </h1>
            <p class="lead px-auto" style="white-space: pre-line;">
                <%-Recipe.description %>
            </p>
            <div class="bi bi"><i class="bi bi-tag text-info">
                    <%=Recipe.category %>
                </i></div>

            <h3>Ingredients</h3>
            <% if (Recipe.ingredients) { %>
                <div class="row-col-12 row-col-md-8 p-4 p-md-2">
                    <ul class="list-group">
                        <% (Recipe.ingredients).forEach(ingredient=> { %>
                            <li class="listègroup-item">
                                <h4>
                                    <%=ingredient%>
                                </h4>
                            </li>
                    </ul>
                    <% }) %>
                        <% } %>

                </div>


        </div>


    </div>






    <% } %>